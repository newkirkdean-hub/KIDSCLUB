# COPR schema (Postgres) — KIDSCLUB

Files added:
- copr_schema.sql — DDL for copr_changers, copr_paidouts, copr_sales

Mapping summary:
- Access "Date With Time" -> PostgreSQL `timestamp without time zone`
- Access "Short Text (n)" -> `varchar(n)`
- Access "Long Text" -> `text`
- Access "Currency" -> `numeric(12,2)`
- IDs -> `integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY` (allows inserting original IDs or letting Postgres autogenerate)

Defaults used:
- All non-ID columns allow NULL by default (no NOT NULL enforced). If you want NOT NULL, specify which columns must be required.
- No foreign keys were created because none were specified in the extracts. If there are relationships, provide them and I will add FKs.
- I prefixed the table names with `db_schema_copr_` to reduce chance of collisions with existing tables; if you prefer the original names (`copr_changers`, `copr_paidouts`, `copr_sales`) tell me and I will rename.

Next steps:
- If this looks good, I can (optionally) rename the tables to the original plain names.
- I can also generate CSV import templates and a migration script to preserve Access metadata (GUID, DateCreated, LastUpdated).
